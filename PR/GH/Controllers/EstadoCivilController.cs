using AutoMapper;
using GH.Extensions;
using GH.Models;
using GH.Models.Persona;
using GH.Repository;
using Microsoft.AspNetCore.Mvc;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace GH.Controllers
{
    //[Route("api/[controller]")]
    //public class EstadoCivilController : BaseApiController<EstadoCivil,>
    //{
    //    public EstadoCivilController(IRepository<EstadoCivil> _repository, IUnitOfWork _unitOfWork, IMapper _mapper) : base(_repository, _unitOfWork, _mapper) { }


    //    [HttpGet]
    //    [Route("{id}")]
    //    public IActionResult Get(int id)
    //    {
    //        return Ok(_repository.FindBy(x => x.EstadoCivilId == id));
    //    }

    //}
}
